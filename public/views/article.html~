{{include './public/header'}}
{{include './public/mheader'}}
			<div class="u-bgt">
				<div class="g-bd2 f-cb">
					<div class="u-btn4 f-fl"></div>
					<div class="st f-fl"><span>装修动态</span></div>
				</div>
			</div>
			<div class="g-bd g-bd3">
					{{each list}}
					{{if $value._id!='56a6ea0e32e975bedc807b06'}}
						<div class="m-demo f-cb">
						    <div class="cnt"><a href="/article/tlp/find/{{$value.id}}">{{$value.title}}</a></div>
						    <div class="date"><img src='{{$value.imgSrc}}'/></div>
						</div>
					{{/if}
					{{/each}}
			</div>
			<div class="bk30"></div>
			</div>
			<div>

			<nav class="m-page">
				{{each pageinfo}}
			    <a href="/article/find/limit/{{$value.url}}" class="z-crt">{{$value.title}}</a>
			    {{/each}}
			</nav>

			</div>
			<div class="bk30"></div>
	</div>
{{include './public/footer'}}